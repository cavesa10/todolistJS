extends includes/layout.pug
block metatags
  title= title
  meta(name="description", content=description)
block content
  main.ToDo
    input#task.taks(type="text", placeholder="Tarea...", spellcheck)
    ul#list.list